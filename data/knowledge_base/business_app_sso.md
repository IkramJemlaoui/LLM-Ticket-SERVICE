# Business Application SSO Login Loops

Before changing access rights, check the identity-provider service status and confirm whether other users are affected. An expired browser session can be cleared for the affected application only, followed by a new login from the approved SSO entry point.

Escalate repeated or multi-user failures to Identity Operations. Never weaken MFA or grant extra permissions as a workaround.

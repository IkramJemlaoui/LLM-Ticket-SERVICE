# Internal DNS Troubleshooting

When public sites work but an internal hostname fails, capture the client DNS server and lookup result, determine the affected network segment, and check the internal DNS record and resolver health.

DNS record changes require normal change control. After an approved correction, flush affected client caches and monitor successful resolution.
